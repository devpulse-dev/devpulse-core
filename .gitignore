/.gradle
/.svn
/.idea
/.run
# Распакованный старый v1-код для справки во время рефакторинга — не коммитим
/.old-src

# Локальный truststore с корпоративными X5 CA — каждый разработчик создаёт сам,
# см. README раздел «SSL: подключение к корпоративному Kaiten».
/local-truststore.jks

# Spring profile с реальными секретами (токены git/kaiten, и т.п.) — каждый
# разработчик копирует из application-local.yml.example и заполняет своими значениями.
**/application-local.yml
**/application-local-*.yml
/*.iml
/**/*.iml
/**/*.log
/assembly-prd/prd-persistent
bo/bo/
mvn.bat
test.sh
# Eclipse
.classpath
.project
.settings/
.metadata/

# Intellij
.idea/
.run/
*.iml
*.iws
*.list

# VSCode
.vscode/

# Mac
.DS_Store

# Maven
log/
target/
*.project
maven-repo/
../bo@tmp/
bo@tmp/
tmp/*
**/*.deb
ci-files/temp/*
ci-files/data/*